<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MyApplication" parent="android:Theme.DeviceDefault.NoActionBar" />

    <!-- Tema splash screen - dipasang di manifest sbg tema awal MainActivity,
         terus di-switch balik ke Theme.MyApplication lewat postSplashScreenTheme
         begitu splash-nya selesai (ditangani otomatis sama library
         androidx.core.splashscreen, dipanggil dari installSplashScreen() di
         MainActivity.kt). -->
    <style name="Theme.Rakku.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.MyApplication</item>
    </style>
</resources>
